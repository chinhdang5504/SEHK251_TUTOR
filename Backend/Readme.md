## Chạy code backend

### 1. Windows
- Mở **Windows PowerShell** trong thư mục chứa `pom.xml`.
- Chạy lệnh:
  ```powershell
  .\mvnw.cmd spring-boot:run


2. Linux
- Mở terminal trong thư mục chứa `pom.xml`.
- Cài đặt maven
-Chạy lệnh:
```bash
  mvn spring-boot:run
